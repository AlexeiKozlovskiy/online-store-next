import { Roboto, Ruluko, Ruge_Boogie, Titillium_Web } from 'next/font/google';

export const ruluko = Ruluko({
  weight: '400',
  subsets: ['latin'],
});

export const rugeBoogie = Ruge_Boogie({
  weight: '400',
  subsets: ['latin'],
});

export const roboto = Roboto({
  weight: '400',
  subsets: ['latin'],
});

export const roboto_bold = Roboto({
  weight: '700',
  subsets: ['latin'],
});

export const titillium = Titillium_Web({
  weight: '400',
  subsets: ['latin'],
});
